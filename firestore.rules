rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // User private document matching: Only the authenticated owner can read & write
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // By default in Firestore, all unmatched paths are denied access
  }
}
